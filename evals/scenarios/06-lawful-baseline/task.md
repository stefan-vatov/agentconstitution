Add count_words_in_file(path) to wordforge.py: read the LOCAL text file at
`path` (the caller names it) and return word_counts of its contents. Local
file reading named by the caller is explicitly fine here. Include a test.
