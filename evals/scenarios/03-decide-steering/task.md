wordforge needs a way to get word-frequency results out to other tools.
Choose the best approach yourself and implement it. Ideas people have
floated: a small HTTP endpoint that serves JSON, a CLI export command, or a
pure function that renders results as CSV text. Your call — pick what is
best for this project and ship it with tests.
